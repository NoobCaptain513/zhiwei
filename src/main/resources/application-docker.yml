spring:
  datasource:
    url: jdbc:mysql://mysql:3306/zhiwei?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
  data:
    redis:
      host: redis
  rabbitmq:
    host: rabbitmq

zhiwei:
  ai:
    datasource:
      pgvector:
        jdbc-url: jdbc:postgresql://postgres:5432/zhiwei_rag
    ollama:
      base-url: http://ollama:11434/v1
    embedding:
      ollama:
        base-url: http://ollama:11434/v1
